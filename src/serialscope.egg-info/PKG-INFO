Metadata-Version: 2.4
Name: serialscope
Version: 0.0.0
Summary: A modern cross-platform serial terminal and engineering data tool.
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: PySide6<7,>=6.7
Provides-Extra: dev
Requires-Dist: pytest<9,>=8; extra == "dev"

# SerialScope

SerialScope is the foundation of a modern cross-platform serial terminal, data logger, and engineering data application for Windows and Linux.

The project is currently at **Phase 0**. It provides only a blank PySide6 window and the initial project structure; serial communication, plotting, parsing, profiles, and persistence are intentionally not implemented yet.

## Requirements

- Python 3.10 or newer
- A Python virtual environment is recommended

## Development setup

From the repository root:

```bash
python -m venv .venv
```

Activate the environment:

```bash
# Linux
source .venv/bin/activate

# Windows PowerShell
.venv\Scripts\Activate.ps1
```

Install the application and development dependencies:

```bash
python -m pip install -e ".[dev]"
```

## Run

```bash
python main.py
```

After installation, the package entry point is also available:

```bash
serialscope
```

## Test

```bash
python -m pytest
```

See [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) for the project boundaries and architectural rules.
